name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          cd chess-game
          npm install

      - name: Build
        run: |
          cd chess-game
          npm run build

      - name: Debug - List directory contents
        run: |
          cd chess-game
          ls -R

      - name: Deploy
        run: |
          git config --global user.name github-actions
          git config --global user.email github-actions@github.com
          git add .
          git commit -m "Deploy chess game" || echo "No changes to commit"
          git push